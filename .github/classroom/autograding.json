{
  "tests": [
    {
      "name": "Run Tests",
      "setup": "mkdir -p build && g++ -std=c++17 -g test/unit_tests.cpp src/*.cpp -o build/test.out -Iinclude -Itest",
      "run": "./build/test.out --no-breaks --success",
      "input": "",
      "output": "[doctest] test cases:",
      "comparison": "included",
      "timeout": 10,
      "points": null
    }
  ]
} 
